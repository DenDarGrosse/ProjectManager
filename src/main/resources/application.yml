spring:
  datasource:
    url: jdbc:h2:mem:projectmanager
    username: application
    password:
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true

jwt:
  token:
    secret: externalintegrationmoduleprojectastratime032023
    expired: 1800000

server:
  error:
    include-message: always
  port: 9090

frontend:
  serverHost: http://localhost
  serverPort: 3000